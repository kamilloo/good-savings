import {Ticker} from "../Ticker";
import {Order} from "../Order";

export class PlaceOrder {

    private symbol:string = 'BCHUSDT';

    place(limit:number, quantity:quantity, side:string):Order{

    }
}
